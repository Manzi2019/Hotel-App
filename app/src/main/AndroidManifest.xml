<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Manzi.myapplication">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Waiter.Menu_Order"></activity>
        <activity android:name=".Admin.Manager_Choice" />
        <activity android:name=".Waiter.Retrive" />
        <activity android:name=".Admin.ManageUser" />
        <activity android:name=".Admin.UpdateMenu" />
        <activity
            android:name=".Admin.ManagerActivity"
            android:label="@string/title_activity_manager"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Waiter.AttendantActivity"
            android:label="@string/title_activity_attendant"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".loginasHeadChefActivity" />
        <activity android:name=".Admin.loginasManagerActivity" />
        <activity android:name=".Waiter.loginAttendantActivity" />
        <activity android:name=".LogINasActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Waiter.OrderSamary" />
    </application>

</manifest>